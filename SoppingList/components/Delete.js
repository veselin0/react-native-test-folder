import React from 'react';
import {Text} from 'react-native';
import styles from './styles.js';

const Delete = () => {
  return <Text style={styles.delete}>Delete Matching</Text>;
};

export default Delete;
